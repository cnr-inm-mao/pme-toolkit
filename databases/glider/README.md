# Glider dataset metadata

This folder contains the repository metadata for the **glider benchmark dataset**.

## Purpose

The metadata stored here documents the dataset used by the glider benchmark cases defined under `benchmarks/`.

## Typical contents

In the current repository snapshot this folder contains `dataset.json`, which records the dataset-level information required by the repository workflow.

## Notes

The glider benchmark is currently the most mature and repository-aligned benchmark case.
