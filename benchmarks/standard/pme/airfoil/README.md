# Standard benchmark — PME — airfoil

This benchmark runs the **standard dataset-selection** workflow for the **airfoil** case using **PME**.

## Files

- `case.json` — main configuration for the dimensionality-reduction run
- `backmapping.json` — example backmapping configuration

## Dataset

This case relies on the **airfoil benchmark dataset** described in:

```text
databases/airfoil/
```

## Notes

The benchmark definition is repository-aligned, but execution still depends on local dataset availability.
