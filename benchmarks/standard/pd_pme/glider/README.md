# Standard benchmark — PD-PME — glider

This benchmark runs the **standard dataset-selection** workflow for the **glider** case using **PD-PME**.

## Files

- `case.json`
- `backmapping.json`

## Dataset

This case relies on the **glider benchmark dataset** described in `databases/glider/`.

## Notes

PD-PME is the physics-driven variant used in the current MATLAB repository workflow for shape optimization.
