# Standard benchmark — PD-PME — propFC

This benchmark defines the **standard dataset-selection** workflow for the **propFC** case using **PD-PME**.

## Files

- `case.json`
- `backmapping.json`

## Dataset

The benchmark definition is present, but public local dataset packaging for `propFC` is not yet aligned with the `glider` and `airfoil` datasets.

## Notes

Verify dataset availability before running the case.
