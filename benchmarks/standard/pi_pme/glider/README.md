# Standard benchmark — PI-PME — glider

This benchmark runs the **standard dataset-selection** workflow for the **glider** case using **PI-PME**.

## Files

- `case.json` — main configuration for the dimensionality-reduction run
- `backmapping.json` — example backmapping configuration

## Dataset

This case relies on the **glider benchmark dataset** described in `databases/glider/`.

## Notes

PI-PME augments the geometric embedding with physical observables.
