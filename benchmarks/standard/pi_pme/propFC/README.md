# Standard benchmark — PI-PME — propFC

This benchmark defines the **standard dataset-selection** workflow for the **propFC** case using **PI-PME**.

## Files

- `case.json`
- `backmapping.json`

## Dataset

The repository contains the benchmark definition, but public local dataset packaging for `propFC` is not yet aligned with `glider` and `airfoil`.

## Notes

Verify dataset availability before running the case.
