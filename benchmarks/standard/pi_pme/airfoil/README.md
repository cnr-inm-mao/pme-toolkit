# Standard benchmark — PI-PME — airfoil

This benchmark runs the **standard dataset-selection** workflow for the **airfoil** case using **PI-PME**.

## Files

- `case.json`
- `backmapping.json`

## Dataset

This case relies on the **airfoil benchmark dataset** described in `databases/airfoil/`.

## Notes

PI-PME augments the geometric embedding with physical observables.
