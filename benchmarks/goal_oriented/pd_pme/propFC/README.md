# Goal-oriented benchmark — PD-PME — propFC

This benchmark defines the **goal-oriented dataset-selection** workflow for the **propFC** case using **PD-PME**.

## Files

- `case.json`
- `backmapping.json`

## Dataset

The benchmark definition is present, but public dataset packaging is not yet aligned with the `glider` and `airfoil` cases.

## Notes

Verify local dataset availability before running the workflow.
