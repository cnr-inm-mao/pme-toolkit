# Goal-oriented benchmark — PD-PME — airfoil

This benchmark runs the **goal-oriented dataset-selection** workflow for the **airfoil** case using **PD-PME**.

## Files

- `case.json`
- `backmapping.json`

## Dataset

This case relies on the **airfoil benchmark dataset** described in `databases/airfoil/`.

## Notes

Goal-oriented selection is configured through the JSON case definition.
