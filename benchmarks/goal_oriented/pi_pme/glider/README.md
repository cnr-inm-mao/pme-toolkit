# Goal-oriented benchmark — PI-PME — glider

This benchmark runs the **goal-oriented dataset-selection** workflow for the **glider** case using **PI-PME**.

## Files

- `case.json`
- `backmapping.json`

## Dataset

This case relies on the **glider benchmark dataset** described in `databases/glider/`.

## Notes

Goal-oriented selection is configured through the JSON case definition.
