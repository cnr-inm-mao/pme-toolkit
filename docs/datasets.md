# Datasets

Dataset metadata currently documented in the repository:

- `databases/glider/`
- `databases/airfoil/`

These folders store repository-level metadata for the corresponding benchmark datasets.

## Notes

The datasets themselves may not always be bundled directly inside the Git repository.
Use the benchmark configuration together with the metadata under `databases/` to understand the required local files.
